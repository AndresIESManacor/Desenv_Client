import {Component} from "react";
import estilFooter from "./footer.css"

class Footer extends Component {
    render() {
        return (
            <div>
                <h1 className={estilFooter.bigblue}>Style Modul</h1>
            </div>
        );
    }
}

export default Footer;